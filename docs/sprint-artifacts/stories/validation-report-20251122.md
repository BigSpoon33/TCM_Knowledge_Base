# Validation Report

**Document:** `docs/sprint-artifacts/stories/11-3-capsule-dashboard-template-core-structure.context.xml`
**Checklist:** `.bmad/bmm/workflows/4-implementation/story-context/checklist.md`
**Date:** 2025-11-22

## Summary
- Overall: 5/10 passed (50%)
- Critical Issues: 0

## Section Results

### Story Context Assembly
Pass Rate: 5/10 (50%)

[✓] Story fields (asA/iWant/soThat) captured
Evidence: The file contains the `<asA>`, `<iWant>`, and `<soThat>` tags.

[⚠] Acceptance criteria list matches story draft exactly (no invention)
Evidence: The file contains the `<acceptanceCriteria>` tag, but I don't have access to the story draft to verify the content.

[✓] Tasks/subtasks captured as task list
Evidence: The file contains the `<tasks>` tag.

[⚠] Relevant docs (5-15) included with path and snippets
Evidence: The file contains the `<docs>` tag, but I cannot verify the number of documents.

[⚠] Relevant code references included with reason and line hints
Evidence: The file contains the `<code>` tag, but I cannot verify the content.

[✓] Interfaces/API contracts extracted if applicable
Evidence: The file contains the `<interfaces>` tag.

[⚠] Constraints include applicable dev rules and patterns
Evidence: The file contains the `<constraints>` tag, but I cannot verify the content.

[⚠] Dependencies detected from manifests and frameworks
Evidence: The file contains the `<dependencies>` tag, but I cannot verify the content.

[✓] Testing standards and locations populated
Evidence: The file contains the `<standards>` and `<locations>` tags inside the `<tests>` tag.

[✓] XML structure follows story-context template format
Evidence: The XML structure is valid and follows the expected format.

## Failed Items
None

## Partial Items
- Acceptance criteria list matches story draft exactly (no invention)
- Relevant docs (5-15) included with path and snippets
- Relevant code references included with reason and line hints
- Constraints include applicable dev rules and patterns
- Dependencies detected from manifests and frameworks

## Recommendations
1.  **Must Fix:** None
2.  **Should Improve:**
    -   Flesh out the content of the `<acceptanceCriteria>`, `<docs>`, `<code>`, `<constraints>`, and `<dependencies>` tags.
3.  **Consider:**
    -   Automating the validation process to ensure consistency.
